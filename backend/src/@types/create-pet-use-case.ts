export enum Age {
  puppy,
  adult,
  senior,
}

export enum Size {
  small,
  medium,
  large,
}

export enum EnergyLevel {
  low,
  medium,
  high,
}

export enum IndependenceLevel {
  low,
  medium,
  high,
}

export enum Environment {
  small,
  medium,
  large,
}
